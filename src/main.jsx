import React from 'react'
import ReactDOM from 'react-dom/client'

import { PiarApp } from './PiarApp.jsx'
import './styles.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <PiarApp />
  </React.StrictMode>,
)
